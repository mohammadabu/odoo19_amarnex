<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Export Attachment Wizard Form View -->
    <record id="view_attachment_export_wizard_form" model="ir.ui.view">
        <field name="name">attachment.export.wizard.form</field>
        <field name="model">attachment.export.wizard</field>
        <field name="arch" type="xml">
            <form string="Export Attachments">
                <group>
                    <field name="filename" readonly="1"/>
                </group>
                <footer>
                    <button name="action_download_zip" 
                            string="Export as ZIP" 
                            type="object" 
                            class="oe_highlight"/>
                    <button string="Cancel" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Export Attachment Action -->
    <record id="action_attachment_export_wizard" model="ir.actions.act_window">
        <field name="name">Export Attachments as ZIP</field>
        <field name="res_model">attachment.export.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_attachment_export_wizard_form"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="base.model_ir_attachment"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
